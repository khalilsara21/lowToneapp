<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sara.lowtoneapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PaginaLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Homepage" />
        <activity android:name=".profile" />
        <activity
            android:name=".RegCitizen"
            android:parentActivityName=".Sing_in" />
        <activity
            android:name=".Sing_in"
            android:parentActivityName=".PaginaLogin" />
        <activity
            android:name=".Status"
            android:parentActivityName=".Homepage" />
        <activity
            android:name=".RegOwner"
            android:parentActivityName=".Sing_in" />
        <activity android:name=".HomepageOwner"></activity>
    </application>

</manifest>